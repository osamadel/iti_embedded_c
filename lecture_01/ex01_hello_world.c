/* Include stdio.h library to use printf function. */
#include <stdio.h>

// Define the main function.
int main(void)
{
    // Calls the printf function and passes a string to it.
    printf("Hello World!\n");
    return 0;
}